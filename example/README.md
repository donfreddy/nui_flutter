# nui_flutter_example

Example application for the `nui_flutter` package.

This is a complete Flutter project generated with Android and iOS runners. It
uses the local package through a path dependency and showcases the available
components, theme tokens, dark mode, and interaction states.

## Run

```sh
cd example
flutter pub get
flutter run
```

## Test

```sh
cd example
flutter test
```
